apiVersion: v1
kind: Service
metadata:
  name: q0717
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "q0717"
    port: 8080
  selector:
    app: q0717
